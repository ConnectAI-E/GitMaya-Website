---
title: GitMaya
description: The power of Tailwind combined with a first-class variant API.
searchable: false
---
import { Hero, Features,Cobe,Actions} from '@components';
import { Callout } from 'nextra-theme-docs';

<Cobe/>

<style jsx global>{`
body {
  background: linear-gradient(
      180deg,
      hsla(0, 0%, 100%, 0) 0,
      #fff min(110vw, 800px)
    ),
    fixed
      repeating-radial-gradient(
        circle at 50% min(calc(50vw + 3.5rem), calc(300px + 7rem)),
        #f5f5f5 0,
        #fff 40px
      );!import
}
.dark body {
  background: linear-gradient(
    0deg,
    hsla(0, 0%, 100%, 0.2) 0,
    #000 min(110vw, 800px)
  ),
  fixed
    repeating-radial-gradient(
      circle at 50% min(calc(50vw + 3.5rem), calc(300px + 5rem)),
      #050505 0,
      #000 40px
    );
}
`}</style>

<Actions/>

